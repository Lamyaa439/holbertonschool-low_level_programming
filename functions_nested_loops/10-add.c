#include "main.h"
/**
* add - adds two integers and returns the result
* @n: first integer
* @m: second integer
*
* Return: sum of a and b
*/

int add(int n, int m)
{
	int add;

	add = n + m;
	return (add);
}
